const initState = {
  userInfo: {},
  token: null,
  errors: null,
  isAuth: false,
};
const userReducer = (state = initState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
export default userReducer;
